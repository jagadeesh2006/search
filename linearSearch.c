#include<stdio.h>
#include <stdlib.h>


linearSearch(int n,int x,int arr[])
{
    for(int i=0;i<n;i++)
    {
        if(a[i]==x)
        {
            return i;
            break;
        }
    
    }
}

int main()
{
    int x,pos;
    scanf("%d%d",&n,&x);
    int a[]={10,20,30,40,50,60,70}
    pos=linearSearch(7,x,arr);
    printf("%d in the %dth position",x,pos);
    


}
